/**
 * Migrate Kubernetes YAML into Kosko components.
 *
 * @packageDocumentation
 */

import { Manifest } from '@kosko/yaml';

export { Manifest }

/**
 * Migrate Kubernetes manifests into a Kosko component.
 *
 * @param data - Array of Kubernetes manifests
 * @public
 */
export declare function migrate(data: readonly Manifest[], options?: MigrateOptions): Promise<string>;

/**
 * @public
 */
export declare enum MigrateFormat {
    /**
     * CommonJS.
     */
    CJS = "cjs",
    /**
     * ECMAScript modules (ESM).
     */
    ESM = "esm"
}

/**
 * @public
 */
export declare interface MigrateOptions {
    /**
     * Output format.
     *
     * @defaultValue Node.js `cjs`
     * @defaultValue Others `esm`
     */
    format?: MigrateFormat;
}

/**
 * Migrate Kubernetes YAML into a kosko component.
 *
 * @param input - Kubernetes YAML string
 * @public
 */
export declare function migrateString(input: string, options?: MigrateOptions): Promise<string>;

export { }
