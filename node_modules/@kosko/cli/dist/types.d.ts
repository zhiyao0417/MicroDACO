/**
 * CLI.
 *
 * @packageDocumentation
 */

/**
 * Handles errors thrown by {@link run} function.
 *
 * @remarks
 * This function prints error message to logger and terminates current process
 * with status code specified in `err` object.
 *
 * @example
 * ```ts
 * run().catch(handleError);
 * ```
 *
 * @public
 */
export declare function handleError(err: unknown): void;

/**
 * Runs CLI with the given arguments.
 *
 * @public
 */
export declare function run(args?: readonly string[]): Promise<void>;

export { }
