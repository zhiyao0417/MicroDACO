import { createTypeMetaGuard, Model, setSchema } from "@kubernetes-models/base";
import { addSchema } from "../../_schemas/IoK8sApiStorageV1beta1StorageClass.mjs";
/**
 * StorageClass describes the parameters for a class of storage for which PersistentVolumes can be dynamically provisioned.
 *
 * StorageClasses are non-namespaced; the name of the storage class according to etcd is in ObjectMeta.Name.
 */
export class StorageClass extends Model {
    constructor(data) {
        super({
            apiVersion: StorageClass.apiVersion,
            kind: StorageClass.kind,
            ...data
        });
    }
}
StorageClass.apiVersion = "storage.k8s.io/v1beta1";
StorageClass.kind = "StorageClass";
StorageClass.is = createTypeMetaGuard(StorageClass);
setSchema(StorageClass, "io.k8s.api.storage.v1beta1.StorageClass", addSchema);
export { StorageClass as IoK8sApiStorageV1beta1StorageClass };
