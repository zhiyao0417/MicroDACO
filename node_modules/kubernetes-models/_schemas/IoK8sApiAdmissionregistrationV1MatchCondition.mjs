import { register } from "@kubernetes-models/validate";
const schema = {
    "properties": {
        "expression": {
            "type": "string"
        },
        "name": {
            "type": "string"
        }
    },
    "required": [
        "name",
        "expression"
    ],
    "type": "object"
};
export function addSchema() {
    register("io.k8s.api.admissionregistration.v1.MatchCondition", schema);
}
