import { register } from "@kubernetes-models/validate";
const schema = {
    "properties": {
        "port": {
            "format": "int32",
            "type": "integer"
        },
        "service": {
            "type": "string",
            "nullable": true
        }
    },
    "required": [
        "port"
    ],
    "type": "object"
};
export function addSchema() {
    register("io.k8s.api.core.v1.GRPCAction", schema);
}
