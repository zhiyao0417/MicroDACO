import { register } from "@kubernetes-models/validate";
const schema = {
    "properties": {
        "replicas": {
            "format": "int32",
            "type": "integer",
            "nullable": true
        }
    },
    "type": "object"
};
export function addSchema() {
    register("io.k8s.api.apps.v1beta2.ScaleSpec", schema);
}
