import { createTypeMetaGuard, Model, setSchema } from "@kubernetes-models/base";
import { addSchema } from "../../_schemas/IoK8sApiNetworkingV1NetworkPolicy.mjs";
/**
 * NetworkPolicy describes what network traffic is allowed for a set of Pods
 */
export class NetworkPolicy extends Model {
    constructor(data) {
        super({
            apiVersion: NetworkPolicy.apiVersion,
            kind: NetworkPolicy.kind,
            ...data
        });
    }
}
NetworkPolicy.apiVersion = "networking.k8s.io/v1";
NetworkPolicy.kind = "NetworkPolicy";
NetworkPolicy.is = createTypeMetaGuard(NetworkPolicy);
setSchema(NetworkPolicy, "io.k8s.api.networking.v1.NetworkPolicy", addSchema);
export { NetworkPolicy as IoK8sApiNetworkingV1NetworkPolicy };
