import { createTypeMetaGuard, Model, setSchema } from "@kubernetes-models/base";
import { addSchema } from "../../_schemas/IoK8sApiBatchV1Job.mjs";
/**
 * Job represents the configuration of a single job.
 */
export class Job extends Model {
    constructor(data) {
        super({
            apiVersion: Job.apiVersion,
            kind: Job.kind,
            ...data
        });
    }
}
Job.apiVersion = "batch/v1";
Job.kind = "Job";
Job.is = createTypeMetaGuard(Job);
setSchema(Job, "io.k8s.api.batch.v1.Job", addSchema);
export { Job as IoK8sApiBatchV1Job };
