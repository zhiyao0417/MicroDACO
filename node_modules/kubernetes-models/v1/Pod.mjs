import { createTypeMetaGuard, Model, setSchema } from "@kubernetes-models/base";
import { addSchema } from "../_schemas/IoK8sApiCoreV1Pod.mjs";
/**
 * Pod is a collection of containers that can run on a host. This resource is created by clients and scheduled onto hosts.
 */
export class Pod extends Model {
    constructor(data) {
        super({
            apiVersion: Pod.apiVersion,
            kind: Pod.kind,
            ...data
        });
    }
}
Pod.apiVersion = "v1";
Pod.kind = "Pod";
Pod.is = createTypeMetaGuard(Pod);
setSchema(Pod, "io.k8s.api.core.v1.Pod", addSchema);
export { Pod as IoK8sApiCoreV1Pod };
