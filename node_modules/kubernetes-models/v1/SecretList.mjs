import { createTypeMetaGuard, Model, setSchema } from "@kubernetes-models/base";
import { addSchema } from "../_schemas/IoK8sApiCoreV1SecretList.mjs";
/**
 * SecretList is a list of Secret.
 */
export class SecretList extends Model {
    constructor(data) {
        super({
            apiVersion: SecretList.apiVersion,
            kind: SecretList.kind,
            ...data
        });
    }
}
SecretList.apiVersion = "v1";
SecretList.kind = "SecretList";
SecretList.is = createTypeMetaGuard(SecretList);
setSchema(SecretList, "io.k8s.api.core.v1.SecretList", addSchema);
export { SecretList as IoK8sApiCoreV1SecretList };
