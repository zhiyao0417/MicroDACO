import { Model, setSchema } from "@kubernetes-models/base";
import { addSchema } from "../_schemas/IoK8sApiCoreV1PodResourceClaim.mjs";
/**
 * PodResourceClaim references exactly one ResourceClaim through a ClaimSource. It adds a name to it that uniquely identifies the ResourceClaim inside the Pod. Containers that need access to the ResourceClaim reference it with this name.
 */
export class PodResourceClaim extends Model {
}
setSchema(PodResourceClaim, "io.k8s.api.core.v1.PodResourceClaim", addSchema);
export { PodResourceClaim as IoK8sApiCoreV1PodResourceClaim };
