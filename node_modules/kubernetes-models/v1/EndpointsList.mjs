import { createTypeMetaGuard, Model, setSchema } from "@kubernetes-models/base";
import { addSchema } from "../_schemas/IoK8sApiCoreV1EndpointsList.mjs";
/**
 * EndpointsList is a list of endpoints.
 */
export class EndpointsList extends Model {
    constructor(data) {
        super({
            apiVersion: EndpointsList.apiVersion,
            kind: EndpointsList.kind,
            ...data
        });
    }
}
EndpointsList.apiVersion = "v1";
EndpointsList.kind = "EndpointsList";
EndpointsList.is = createTypeMetaGuard(EndpointsList);
setSchema(EndpointsList, "io.k8s.api.core.v1.EndpointsList", addSchema);
export { EndpointsList as IoK8sApiCoreV1EndpointsList };
