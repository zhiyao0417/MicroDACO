import { createTypeMetaGuard, Model, setSchema } from "@kubernetes-models/base";
import { addSchema } from "../_schemas/IoK8sApiCoreV1Service.mjs";
/**
 * Service is a named abstraction of software service (for example, mysql) consisting of local port (for example 3306) that the proxy listens on, and the selector that determines which pods will answer requests sent through the proxy.
 */
export class Service extends Model {
    constructor(data) {
        super({
            apiVersion: Service.apiVersion,
            kind: Service.kind,
            ...data
        });
    }
}
Service.apiVersion = "v1";
Service.kind = "Service";
Service.is = createTypeMetaGuard(Service);
setSchema(Service, "io.k8s.api.core.v1.Service", addSchema);
export { Service as IoK8sApiCoreV1Service };
