import { createTypeMetaGuard, Model, setSchema } from "@kubernetes-models/base";
import { addSchema } from "../_schemas/IoK8sApiCoreV1NodeList.mjs";
/**
 * NodeList is the whole list of all Nodes which have been registered with master.
 */
export class NodeList extends Model {
    constructor(data) {
        super({
            apiVersion: NodeList.apiVersion,
            kind: NodeList.kind,
            ...data
        });
    }
}
NodeList.apiVersion = "v1";
NodeList.kind = "NodeList";
NodeList.is = createTypeMetaGuard(NodeList);
setSchema(NodeList, "io.k8s.api.core.v1.NodeList", addSchema);
export { NodeList as IoK8sApiCoreV1NodeList };
