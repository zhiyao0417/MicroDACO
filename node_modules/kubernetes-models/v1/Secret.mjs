import { createTypeMetaGuard, Model, setSchema } from "@kubernetes-models/base";
import { addSchema } from "../_schemas/IoK8sApiCoreV1Secret.mjs";
/**
 * Secret holds secret data of a certain type. The total bytes of the values in the Data field must be less than MaxSecretSize bytes.
 */
export class Secret extends Model {
    constructor(data) {
        super({
            apiVersion: Secret.apiVersion,
            kind: Secret.kind,
            ...data
        });
    }
}
Secret.apiVersion = "v1";
Secret.kind = "Secret";
Secret.is = createTypeMetaGuard(Secret);
setSchema(Secret, "io.k8s.api.core.v1.Secret", addSchema);
export { Secret as IoK8sApiCoreV1Secret };
