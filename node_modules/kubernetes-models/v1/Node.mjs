import { createTypeMetaGuard, Model, setSchema } from "@kubernetes-models/base";
import { addSchema } from "../_schemas/IoK8sApiCoreV1Node.mjs";
/**
 * Node is a worker node in Kubernetes. Each node will have a unique identifier in the cache (i.e. in etcd).
 */
export class Node extends Model {
    constructor(data) {
        super({
            apiVersion: Node.apiVersion,
            kind: Node.kind,
            ...data
        });
    }
}
Node.apiVersion = "v1";
Node.kind = "Node";
Node.is = createTypeMetaGuard(Node);
setSchema(Node, "io.k8s.api.core.v1.Node", addSchema);
export { Node as IoK8sApiCoreV1Node };
