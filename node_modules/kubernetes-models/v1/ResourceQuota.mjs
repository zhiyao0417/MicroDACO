import { createTypeMetaGuard, Model, setSchema } from "@kubernetes-models/base";
import { addSchema } from "../_schemas/IoK8sApiCoreV1ResourceQuota.mjs";
/**
 * ResourceQuota sets aggregate quota restrictions enforced per namespace
 */
export class ResourceQuota extends Model {
    constructor(data) {
        super({
            apiVersion: ResourceQuota.apiVersion,
            kind: ResourceQuota.kind,
            ...data
        });
    }
}
ResourceQuota.apiVersion = "v1";
ResourceQuota.kind = "ResourceQuota";
ResourceQuota.is = createTypeMetaGuard(ResourceQuota);
setSchema(ResourceQuota, "io.k8s.api.core.v1.ResourceQuota", addSchema);
export { ResourceQuota as IoK8sApiCoreV1ResourceQuota };
