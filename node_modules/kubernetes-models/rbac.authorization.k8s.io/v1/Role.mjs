import { createTypeMetaGuard, Model, setSchema } from "@kubernetes-models/base";
import { addSchema } from "../../_schemas/IoK8sApiRbacV1Role.mjs";
/**
 * Role is a namespaced, logical grouping of PolicyRules that can be referenced as a unit by a RoleBinding.
 */
export class Role extends Model {
    constructor(data) {
        super({
            apiVersion: Role.apiVersion,
            kind: Role.kind,
            ...data
        });
    }
}
Role.apiVersion = "rbac.authorization.k8s.io/v1";
Role.kind = "Role";
Role.is = createTypeMetaGuard(Role);
setSchema(Role, "io.k8s.api.rbac.v1.Role", addSchema);
export { Role as IoK8sApiRbacV1Role };
